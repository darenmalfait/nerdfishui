---
title: 'Code Block'
description: 'Syntax-highlighted code with a Blume-style header bar and overlay copy control.'
sidebar:
  label: 'Code Block'
  order: 15
  badge: 'New'
---

<Component path="code-block/default" />

## With Header

Pass `title` (or just `language`) for the top bar — or `actions` alone. Brand language icons load on demand (`icon={false}` to hide, or pass a custom node). Pass `header={false}` to highlight without chrome. Copy sits overlaid on the right.

<Component path="code-block/with-header" />

## Custom Copy Button

Use the `actions` prop to replace the default overlay copy control.

<Component path="code-block/custom-copy" />
