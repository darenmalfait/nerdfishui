<DocsPageHeader
	title="Popover"
	description="Popover is a component that displays a list of items in a popover"
	githubPath={{
		package: 'ui',
		path: 'components/popover.tsx',
	}}
/>

<ComponentExample
	component={<PopoverExample />}
	src="/src/app/docs/components/popover/example/default.tsx"
/>

## Usage

```tsx
import { Popover, PopoverTrigger, PopoverContent } from '@nerdfish/ui'
```

```tsx
<Popover>
	<PopoverTrigger>Open</PopoverTrigger>
	<PopoverContent>Popover content</PopoverContent>
</Popover>
```
