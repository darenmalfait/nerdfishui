import { EmptyExample } from './example/default'

<DocsPageHeader
	title="Empty"
	description="Empty is a component that can be used to display a message there is no data to display."
	githubPath={{
		package: 'react',
		path: 'components/empty/empty.tsx',
	}}
/>

<ComponentExample component={EmptyExample} name="default" />
