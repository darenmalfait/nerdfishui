import { ContextMenuExample } from './example/default'

<DocsPageHeader
	title="ContextMenu"
	description="Displays a menu to the user — such as a set of actions or functions — triggered by a button."
	githubPath={{
		package: 'react',
		path: 'src/components/context-menu/context-menu.tsx',
	}}
/>

<ComponentExample component={ContextMenuExample} name="default" />
