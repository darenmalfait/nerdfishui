import { CheckboxExample } from './example/default'

<DocsPageHeader
	title="Checkbox"
	description="A control that allows the user to toggle between checked and not checked."
	githubPath={{
		package: 'react',
		path: 'src/components/checkbox/checkbox.tsx',
	}}
/>

<ComponentExample component={CheckboxExample} name="default" />
