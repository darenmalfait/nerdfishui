import { SelectExample } from './example/default'

<DocsPageHeader
	title="Select"
	description="Displays a list of options for the user to pick from—triggered by a button."
	githubPath={{
		package: 'react',
		path: 'components/select/select.tsx',
	}}
/>

<ComponentExample component={SelectExample} name="default" />
