export const metaTitle = 'Button'
export const description = 'Button component of Nerdfishui'
export const pkg = 'ui'
export const path = 'button'

# Button

Button is a component that is used to trigger an action or event, such as
submitting a form, opening a dialog, canceling an action, or performing a delete
operation. If you want a link that looks like a button, go to
[ButtonLink](/button-link).

<Code code={`import { Button } from '@nerdfish/ui'`} />

## Preview

<Preview code={`<Button>Click me</Button>`}>
  <div className="space-y-2">
    <Button>Click me</Button>
  </div>
</Preview>

---

## Properties

These properties extend
[HTMLButtonElement](https://developer.mozilla.org/en-US/docs/Web/API/HTMLButtonElement)

<Properties>
  <Property
    name="variant"
    type={[
      'default',
      'danger',
      'success',
      'outline',
      'subtle',
      'ghost',
      'link',
    ]}
  >
    styling preset for the button.
  </Property>
  <Property name="size" type={['default', 'sm', 'lg']}>
    The size of the button.
  </Property>
</Properties>

---

## variant

<Preview
  code={`<Button>Button</Button>
<Button variant="default">Default</Button>
<Button variant="danger">Danger</Button>
<Button variant="success">Success</Button>
<Button variant="outline">Outline</Button>
<Button variant="subtle">Subtle</Button>
<Button variant="ghost">Ghost</Button>
<Button variant="link">Link</Button>`}
>
  <div className="flex w-full flex-wrap gap-2">
    <Button>Button</Button>
    <Button variant="default">Default</Button>
    <Button variant="danger">Danger</Button>
    <Button variant="success">Success</Button>
    <Button variant="outline">Outline</Button>
    <Button variant="subtle">Subtle</Button>
    <Button variant="ghost">Ghost</Button>
    <Button variant="link">Link</Button>
  </div>
</Preview>

## size

<Preview
  code={`<Button>Button</Button>
<Button size="default">Button</Button>
<Button size="sm">Button</Button>
<Button size="lg">Button</Button>`}
>
  <div className="flex w-full flex-wrap gap-2 items-center">
    <Button size="default">Button</Button>
    <Button size="sm">Button</Button>
    <Button size="lg">Button</Button>
  </div>
</Preview>
